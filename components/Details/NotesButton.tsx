import { View, Text, Pressable } from "react-native";
import React from "react";

const NotesButton = () => {
  return (
    <Pressable>
      <Text>Add note</Text>
    </Pressable>
  );
};

export default NotesButton;
